import { PrismaClient } from "@prisma/client";
import bcrypt from "bcryptjs";
const prisma = new PrismaClient();
const initial = { profile:{name:"Ayo Okafor",title:"Filmmaker & Video Editor",bio:"I shape stories with image, rhythm and intention.",about:"A visual storyteller working across film, music, culture and brand.",email:"hello@ayo.example",heroImage:"",profileImage:"",skills:["Direction","Cinematography","Editing"],socials:{instagram:"",vimeo:"",linkedin:""},cvUrl:"",cvVisible:true}, folders:[{id:"reel",name:"Selected work",hidden:false,order:0}], projects:[{id:"first-frame",folderId:"reel",title:"The first frame",summary:"A cinematic study in light, movement and place.",client:"Personal",year:"2026",role:"Director / Editor",thumbnail:"",videoUrl:"",featured:true,visible:true,order:0}], settings:{metaDescription:"A cinematic portfolio",showContact:true} };
async function main(){ const email=process.env.SEED_ADMIN_EMAIL || "admin@example.com"; const password=process.env.SEED_ADMIN_PASSWORD || "change-me-now"; const user=await prisma.user.upsert({where:{email},update:{},create:{email,passwordHash:await bcrypt.hash(password,12)}}); await prisma.portfolio.upsert({where:{userId:user.id},update:{},create:{userId:user.id,slug:"ayo",draft:initial,published:initial,publishedAt:new Date()}}); }
main().finally(()=>prisma.$disconnect());
